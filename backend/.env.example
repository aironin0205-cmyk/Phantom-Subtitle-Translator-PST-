// ===== PRODUCTION-READY .env.example =====
// This file is a template for the environment variables required by the application.
// To run the project locally, follow these steps:
// 1. Copy this file to a new file named `.env` in the root of the 'backend' directory.
// 2. Fill in the actual values for your secret keys and connection strings in the new `.env` file.
// IMPORTANT: The .env file is listed in .gitignore and MUST NEVER be committed to version control.


# ========================================
# SECTION 1: SERVER & DEPLOYMENT SETTINGS
# ========================================

# The port the application will listen on.
# Default: 3001
PORT=3001

# The URL of your live frontend application (e.g., from Vercel or Netlify).
# This is CRITICAL for the CORS security policy to allow your frontend to make API calls.
# For local development with a React/Vue/Svelte frontend, this is a common default.
FRONTEND_URL="http://localhost:5173"


# ========================================
# SECTION 2: API KEYS & SECRETS (REQUIRED)
# ========================================

# --- Google Gemini ---
# Get your API key from Google AI Studio: https://aistudio.google.com/app/apikey
GEMINI_API_KEY="YOUR_GEMINI_API_KEY_HERE"

# --- MongoDB ---
# Get your connection string from a MongoDB Atlas free-tier cluster.
# Ensure your IP address is whitelisted in Atlas Network Access settings.
MONGO_URI="YOUR_MONGODB_CONNECTION_STRING_HERE"

# --- Pinecone ---
# Get your API key from the Pinecone dashboard.
PINECONE_API_KEY="YOUR_PINECONE_API_KEY_HERE"
# The name of the Pinecone index the application will use.
PINECONE_INDEX_NAME="pst-translations"


# ========================================
# SECTION 3: APPLICATION TUNING (OPTIONAL)
# ========================================

# --- Logging Level ---
# The minimum level of logs to display.
# Options (from least to most verbose): 'fatal', 'error', 'warn', 'info', 'debug', 'trace'
# 'debug' is recommended for local development to see detailed logs.
# 'info' is the standard for production environments.
LOG_LEVEL=debug


# --- Gemini Model Selection ---
# Allows you to easily swap models without changing code.
# Use the latest and most powerful models available for best results.
GEMINI_BLUEPRINT_MODEL="gemini-2.5-pro-latest"
GEMINI_TRANSLATION_MODEL="gemini-2.5-pro-latest"
GEMINI_SYNC_MODEL="gemini-2.5-flash-latest"


# --- Gemini Retry Logic ---
# The maximum number of times to retry a failed API call to Gemini.
GEMINI_MAX_RETRIES=3
# The initial delay (in milliseconds) for the exponential backoff on retries.
GEMINI_BACKOFF_MS=200


# --- Rate Limiting ---
# Maximum number of requests allowed from a single IP in the time window.
RATE_LIMIT_MAX_REQUESTS=100
# The time window in milliseconds (e.g., 60000 = 1 minute).
RATE_LIMIT_WINDOW_MS=60000
