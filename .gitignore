# =========================================
# ===    NODE.JS .GITIGNORE TEMPLATE    ===
# =========================================
# This file tells Git which files and folders to permanently ignore.

# --- Dependencies ---
# The node_modules directory contains all of the project's dependencies.
# This directory is generated by running `npm install` and should never be committed.
/node_modules

# --- Secrets ---
# The .env file contains sensitive API keys and connection strings for local development.
# It MUST be kept private. The .env.example file, which is a template, should be committed.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- Logs ---
# Log files generated by npm, yarn, or the application itself.
# These can be verbose and are specific to a single run.
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Build & Cache ---
# Directories for compiled output and caches from build tools.
/dist
/build
.vercel
.next
.nuxt

# --- Testing ---
# Directory for code coverage reports generated by testing frameworks like Jest.
/coverage

# --- IDE & Editor Config ---
# User-specific settings for IDEs and code editors.
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- Operating System Files ---
# Metadata files generated by macOS and Windows.
.DS_Store
Thumbs.db
